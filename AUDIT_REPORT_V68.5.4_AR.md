# تقرير فحص منصة المستر سعد عويضة — V68.5.4

## النتيجة

تم فحص صفحات المنصة، ملفات JavaScript وJSON، المراجع المحلية، أزرار الواجهة،
صلاحيات Firestore، Cloud Functions، الحجز، الحسابات، بوابات الطالب وولي الأمر،
الاختبارات، الحضور، المدفوعات، النسخ الاحتياطي، PWA والبناء النهائي.

## الأخطاء التي تم إصلاحها

1. كان منع التكرار يعتمد على رقم طلب محفوظ في المتصفح، ولذلك كان يمكن للطالب
   التسجيل مرة أخرى من جهاز أو متصفح آخر.
2. لم يكن هناك قفل مركزي يمنع عمليتي تسجيل متزامنتين لنفس الاسم.
3. إضافة الطالب من لوحة الإدارة أو الاستيراد كان يمكن أن يتجاوز منع التكرار.
4. السجلات القديمة التي لا تحتوي على مفتاح الاسم لم تكن مشمولة بأي فحص موحّد.
5. رسائل الخطأ لم تكن تشرح أن الطالب مسجل بالفعل وما الإجراء الصحيح.
6. كان رقم إصدار قديم مستخدمًا داخل وسم نشر Cloud Functions.
7. كان ملف قفل Functions يحتوي على مرجع زائد للمشروع الأب، وقد يسبب مشكلات
   تثبيت أو تحليل أثناء النشر.

## منع تسجيل الطالب المكرر

- لا يقبل التسجيل أقل من ثلاثة مقاطع اسم واضحة.
- إذا كان الاسم الثلاثي مستخدمًا لطالب آخر، يجب إضافة الاسم الرابع لتمييز الطالبين.
- يُطبّع الاسم العربي قبل المقارنة: المسافات المتكررة، التشكيل، التطويل،
  أشكال الألف و`ى/ي`.
- يُنشأ مفتاح مشفر ثابت للاسم داخل `_student_name_claims`.
- إنشاء القفل وحساب الطالب والحجز يتمان ذريًا؛ إما ينجحان معًا أو لا يُحفظ شيء.
- يشمل الحجز العام، إضافة طالب سنتر، إضافة طالب أونلاين، والاستيراد.
- تتم مراجعة السجلات القديمة حتى لو لم يكن `nameKey` موجودًا بها.
- عند رفض الحجز أو حذف الطالب بأمان يُحرر قفل الاسم للسماح بالتسجيل الصحيح لاحقًا.
- قواعد Firestore تمنع تعديل قفل اسم موجود أو تجاوزه من العميل.

## نتائج التحقق

- جميع اختبارات المشروع: ناجحة.
- اختبارات تطبيع ومطابقة الأسماء: ناجحة.
- جميع صفحات HTML والمراجع المحلية: ناجحة.
- جميع إجراءات الواجهة المعرفة: ناجحة.
- فحوص الصلاحيات والحجز والامتحانات والنسخ الاحتياطي: ناجحة.
- فحوص PWA على Android وiPhone: ناجحة.
- بناء `dist`: ناجح ولا يحتوي على Backend أو ملفات بيئة.
- تدقيق حزم المشروع وFunctions: صفر ثغرات معروفة.

## النشر المطلوب

هذا الإصدار يغير Cloud Functions وقواعد Firestore والواجهة، ولذلك يجب نشر
الخلفية والقواعد والاستضافة معًا:

```powershell
npm install
npm --prefix functions install
npm test
npm run build
firebase deploy --only firestore:rules,firestore:indexes,storage,functions,hosting
```

بعد النشر، اختبر اسمًا موجودًا من نافذة خاصة. يجب رفض الطلب برسالة:
`هذا الطالب مسجل بالفعل على المنصة`.
